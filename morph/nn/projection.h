namespace morph {
    namespace nn {
        struct projection
        {
            population* from;
            population* to;
            weights* w;
            postsynapse* ps;
        };
    }
}
