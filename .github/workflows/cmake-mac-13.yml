name: Mac OS 13
on:
#  push:
#    branches: [ "main" ]
#    paths-ignore:
#      - 'docs/**'
#      - 'README**'
#  pull_request:
#    branches: [ "main" ]
# If not the above, then:
  workflow_dispatch:
    inputs:
      tags:
        description: 'Test scenario tags'
        required: false
        type: boolean

jobs:
  call-workflow-passing-data:
    uses: ABRG-Models/morphologica/.github/workflows/cmake-mac-template.yml@main
    with:
      CONTAINER_IMAGE: macos-14
      BUILD_TYPE: Release
      CC: clang-14
      CXX: clang++-14
