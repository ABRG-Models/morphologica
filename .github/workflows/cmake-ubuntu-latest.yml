name: Ubuntu latest
on:
#  push:
#    branches: [ "main" ]
#    paths-ignore:
#      - 'docs/**'
#      - 'README**'
#  pull_request:
#    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      tags:
        description: 'Test scenario tags'
        required: false
        type: boolean

jobs:
  call-workflow-passing-data:
    uses: rremilian/morphologica/.github/workflows/cmake-ubuntu-template.yml@github_actions_improvments
    with:
      CONTAINER_IMAGE: ubuntu-latest
      BUILD_TYPE: Release
      CC: gcc
      CXX: g++
